[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

# SPA: qualquer rota (ex.: /admin) carrega o site
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
